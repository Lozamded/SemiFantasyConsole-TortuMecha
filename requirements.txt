# TortoisePlayer / tortoisengine — rendering, input, audio
pygame>=2.6.0
numpy>=1.24.0

# TortoiseStudio — editor chrome (viewport stays Pygame)
PyQt6>=6.6.0

# Reload scripts when edited in Cursor / VSCode / etc.
watchdog>=4.0.0

# Native "build executable" export from TortoiseStudio (see tortoisengine/build_executable.py)
pyinstaller>=6.0.0
