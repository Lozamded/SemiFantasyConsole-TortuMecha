"""Live variable Save_overwrite.tortuguilayer's GUI.csv "overwrite" string
reads via [var<[current_slot]>] (see tortuengine.localization) — bound by
Save_hud.py's init() since both GUI layers are only ever active together,
in save_scene."""

current_slot: int = 1
